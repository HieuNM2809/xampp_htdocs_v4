# Kafka Users Configuration
# Format: username=password

# Admin users
admin=admin-secret
kafka-admin=kafka-admin-secret

# Application users
producer=producer-secret
consumer=consumer-secret
nodejs-app=nodejs-app-secret

# Demo/Test users
demo-user=demo-password
test-user=test-secret

# Service accounts
api-service=api-service-secret
monitor-service=monitor-secret
